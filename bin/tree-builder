#! /usr/bin/env python3

import json
import os

files = []

for root, dirs, fs in os.walk('./'):
    files.extend([
        {
            'path': os.path.join(root, f),
            'changed': root == './tf'
        }
        for f in fs])

print(json.dumps(files))
