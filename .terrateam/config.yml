cost_estimation:
  enabled: false

engine:
  name: terraform
  version: 1.3.7

workflows:
  - tag_query: ''
    plan:
      - type: run
        cmd: ['terraform', 'fmt', '-diff', '-check']
      - type: init
      - type: plan

parallel_runs: 10

batch_runs:
  enabled: true
  max_workspaces_per_batch: 3
