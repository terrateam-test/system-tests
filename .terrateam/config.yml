engine:
  name: tofu
hooks:
  all:
    pre:
    - cmd:
      - echo
      - resource
      name: NOT_SENSITIVE
      type: env
    - cmd:
      - ./bin/env
      method: source
      sensitive: true
      type: env
