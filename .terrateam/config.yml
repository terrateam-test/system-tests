engine:
  name: tofu
stacks:
  names:
    databases:
      stacks:
        - database1
        - database2
      rules:
        modified_by:
          - base
    database1:
      tag_query: 'dir:database1'
    database2:
      tag_query: 'dir:database2'
    base:
      tag_query: 'dir:base'
    webservice:
      tag_query: 'dir:webservice'
      rules:
        modified_by:
          - databases
