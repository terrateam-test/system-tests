access_control:
  enabled: true
  policies:
  - plan:
    - '*'
    apply_force: ['*']
    tag_query: ''
apply_requirements:
  checks:
    - tag_query: ''
      approved:
        enabled: true
        any_of_count: 1
engine:
  name: tofu
when_modified:
  autoplan: false
