stacks:
  names:
    mystack:
      tag_query: ''
      variables:
        ENV: production

engine:
  name: tofu

workflows:
  - tag_query: 'stack_name:mystack'
    environment: '${ENV}'
    plan:
      - type: run
        cmd: ['thisprogramdoesnotexist']
